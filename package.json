{
  "name": "proyecto-final-carnes-al-barril",
  "private": true,
  "version": "1.0.0",
  "description": "Arranca backend (Express) y frontend (Vite) juntos",
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --prefix backend\" \"npm run dev --prefix frontend\"",
    "start": "npm run dev"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
